[package]
name = "mcp-servers-rust"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
github-copilot-sdk = { path = "../../../../../rust" }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
