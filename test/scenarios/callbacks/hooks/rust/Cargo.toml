[package]
name = "hooks-rust"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
github-copilot-sdk = { path = "../../../../../rust" }
async-trait = "0.1"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "sync"] }
